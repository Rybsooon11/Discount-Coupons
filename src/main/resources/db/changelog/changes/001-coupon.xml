<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <changeSet id="001-create-coupon" author="coupons">
        <createTable tableName="coupon">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="code" type="VARCHAR(64)">
                <constraints nullable="false" unique="true" uniqueConstraintName="uq_coupon_code"/>
            </column>
            <column name="country_code" type="VARCHAR(2)">
                <constraints nullable="false"/>
            </column>
            <column name="max_uses" type="INT">
                <constraints nullable="false"/>
            </column>
            <column name="current_uses" type="INT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="now()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <sql>ALTER TABLE coupon ADD CONSTRAINT chk_coupon_max_uses_positive CHECK (max_uses &gt; 0);</sql>
        <sql>ALTER TABLE coupon ADD CONSTRAINT chk_coupon_current_uses_nonneg CHECK (current_uses &gt;= 0);</sql>
        <sql>ALTER TABLE coupon ADD CONSTRAINT chk_coupon_uses_bounds CHECK (current_uses &lt;= max_uses);</sql>

        <createIndex tableName="coupon" indexName="idx_coupon_code">
            <column name="code"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
